# Pre-run status

No history candidate is approved before the workflow commits a generated
`latest/summary.json`. Native ARM64 candidate build, package promotion, and ISO
assembly are therefore disabled at this checkpoint.
