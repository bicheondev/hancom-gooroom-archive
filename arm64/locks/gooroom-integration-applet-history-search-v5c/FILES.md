# Generated `latest/` authority

A completed run may commit the closest comparison, differing-function table,
reconstruction report and patch, source lock, aggregate summary, file inventory,
checksums, and artifact lock. Missing optional files do not imply success; the
aggregate summary is authoritative and remains fail-closed.
