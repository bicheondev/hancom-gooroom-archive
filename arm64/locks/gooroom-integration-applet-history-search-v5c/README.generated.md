The generated `latest/` directory is intentionally absent before the first
successful aggregate run. Its summary must remain fail-closed unless a candidate
has zero resource, normalized dynamic-function, and non-ELF payload differences.
