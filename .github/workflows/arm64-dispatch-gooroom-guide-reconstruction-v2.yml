name: ARM64 port - dispatch final gooroom-guide ELF diagnostics v4

on:
  push:
    branches: [arm64-port]
    paths:
      - '.github/workflows/arm64-dispatch-gooroom-guide-reconstruction-v2.yml'
  workflow_dispatch:

permissions:
  actions: write
  contents: read

concurrency:
  group: arm64-dispatch-final-gooroom-guide-elf-diagnostics-v4
  cancel-in-progress: false

jobs:
  dispatch:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Dispatch the retained final AMD64 ELF diagnostic workflow
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euxo pipefail
          for attempt in 1 2 3 4 5 6; do
            if gh workflow run \
              arm64-diagnose-gooroom-guide-amd64-elf.yml \
              --repo "$GITHUB_REPOSITORY" \
              --ref arm64-port; then
              exit 0
            fi
            sleep $((attempt * 3))
          done
          exit 1
