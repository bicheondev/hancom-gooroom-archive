name: ARM64 port - final signed DSC pipeline integration v9

on:
  push:
    branches: [arm64-port]
    paths:
      - '.github/workflows/arm64-source-authority-v9-maintenance.yml'
      - 'arm64/scripts/wire_source_authority_v9.py'
  workflow_dispatch:

permissions:
  contents: write
  actions: write

concurrency:
  group: hancom-gooroom-33-source-authority-integration-v3
  cancel-in-progress: true

jobs:
  integrate:
    runs-on: ubuntu-24.04
    timeout-minutes: 55
    steps:
      - name: Check out the complete port branch
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          ref: arm64-port
          fetch-depth: 0

      - name: Cancel all older integration writers and reset after they settle
        shell: bash
        run: |
          set -euxo pipefail
          sleep 540
          git fetch origin arm64-port
          git reset --hard origin/arm64-port

      - name: Apply the complete idempotent v9 integration
        shell: bash
        run: |
          set -euxo pipefail
          python3 -m py_compile \
            arm64/scripts/wire_source_authority_v3.py \
            arm64/scripts/wire_source_authority_v4.py \
            arm64/scripts/wire_source_authority_v5.py \
            arm64/scripts/wire_source_authority_v6.py \
            arm64/scripts/wire_source_authority_v7.py \
            arm64/scripts/wire_source_authority_v8.py \
            arm64/scripts/wire_source_authority_v9.py \
            arm64/scripts/collect_native_rebuild_results_v3.py \
            arm64/scripts/collect_native_rebuild_results_v4.py
          for script in \
            arm64/scripts/build_locked_source_arm64_v5.sh \
            arm64/scripts/build_locked_dsc_source_arm64_v2.sh \
            arm64/scripts/build_locked_dsc_source_arm64.sh; do
            bash -n "$script"
          done
          python3 arm64/scripts/wire_source_authority_v9.py \
            --repository-root .

      - name: Remove obsolete authorities and every one-shot integration patcher
        shell: bash
        run: |
          set -euxo pipefail
          obsolete=(
            .github/workflows/arm64-effective-source-lock.yml
            .github/workflows/arm64-publish-rebuild-packages.yml
            .github/workflows/arm64-source-authority-v3-maintenance.yml
            .github/workflows/arm64-source-authority-v5-maintenance.yml
            .github/workflows/arm64-source-authority-v7-maintenance.yml
            .github/workflows/arm64-source-authority-v8-maintenance.yml
            .github/workflows/arm64-source-authority-v9-maintenance.yml
            arm64/scripts/build_locked_source_arm64_v3.sh
            arm64/scripts/build_locked_source_arm64_v4.sh
            arm64/scripts/wire_source_authority_v3.py
            arm64/scripts/wire_source_authority_v4.py
            arm64/scripts/wire_source_authority_v5.py
            arm64/scripts/wire_source_authority_v6.py
            arm64/scripts/wire_source_authority_v7.py
            arm64/scripts/wire_source_authority_v8.py
            arm64/scripts/wire_source_authority_v9.py
          )
          for path in "${obsolete[@]}"; do
            git rm -f "$path" 2>/dev/null || rm -f "$path"
          done

      - name: Commit one active exact Git-or-DSC execution graph
        shell: bash
        run: |
          set -euxo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add .github/workflows arm64/scripts
          if git diff --cached --quiet; then
            echo 'The complete v9 graph was already active.'
            exit 0
          fi
          git commit -m 'Finalize exact Git and signed DSC ARM64 pipeline v9'
          for attempt in 1 2 3 4; do
            git pull --rebase origin arm64-port
            if git push origin HEAD:arm64-port; then
              exit 0
            fi
            sleep $((attempt * 4))
          done
          exit 1

      - name: Dispatch recovery, audits, rebuilds, publication, and acquisition
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euxo pipefail
          dispatch() {
            local workflow="$1"
            shift
            for attempt in 1 2 3; do
              if gh workflow run "$workflow" \
                --repo '${{ github.repository }}' \
                --ref arm64-port "$@"; then
                return 0
              fi
              sleep $((attempt * 5))
            done
            return 1
          }
          dispatch arm64-source-keyring-lock.yml
          dispatch arm64-wayback-source-recovery.yml || true
          dispatch arm64-source-authority-v3.yml || true
          dispatch arm64-workflow-secret-audit.yml
          dispatch arm64-active-pipeline-audit.yml
          dispatch arm64-pipeline-lint.yml
          dispatch arm64-pipeline-unit-tests.yml
          dispatch arm64-baseline-rebuild-evidence.yml || true
          dispatch arm64-native-rebuild-backlog.yml -f limit=6 || true
          dispatch arm64-rebuild-failure-audit.yml || true
          dispatch arm64-publish-rebuild-packages-v3.yml || true
          dispatch arm64-package-acquisition-plan-v2.yml || true
          dispatch arm64-port-coverage.yml || true
          dispatch arm64-ci-state-snapshot.yml || true
